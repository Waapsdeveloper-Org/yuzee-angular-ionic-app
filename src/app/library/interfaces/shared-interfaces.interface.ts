export interface LocationObjInterface {
    address: string;
    latitude: number;
    longitude: number;
  }

  export interface rangeV {
    min_value: number;
    max_value: number;
    selected_min_value: number;
    selected_max_value: number;
    reset: boolean;
  }